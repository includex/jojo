# 영천 전투 파리티 작업 요약 (2026-09-20)

원본 Cocos 게임과 Kotlin/LibGDX 이식본을 영천 전투 중심으로 검증하고 고친 기록이다.
자세한 경위는 `YINGCHUAN_PARITY_PROGRESS.md`에 이어 적혀 있고, 이 문서는 한 장 요약이다.

## 결과

- **커밋 24개**, 포트 결함 **17건** 수정
- **영천 전투 렌더 경로 27개 전부 통과**
- **색 비교 28행 → 909행 (100%)**
- **구간 타이밍 대조 5건 통과** (첫 교전 / 다음 행동 / 적 첫 교전 / 477 정산 / round3-210)
- 두 전체 테스트 스위트 초록 (`:core:test`, `:verification:test`)
- 미완 작업(`BattleScreen.kt` 등 424/56)은 **손대지 않고 보존**

## 고친 포트 결함

### 화면이 통째로 빠져 있던 것

| 화면 | 빠진 것 | 원본 근거 |
| --- | --- | --- |
| 아이템 상세 | "장착 가능한 부대" 13×3 표 + 머리띠 3장 = **55개 노드** | `ui/ItemLayer.js:122-131` |

### 기하가 어긋난 것

| 화면 | 포트 | 원본 |
| --- | --- | --- |
| 지형 정보창 안쪽 패널 | (288, 184, 993×460) | (285.538, 183.098, 1001.1×459.3) |
| 지형 정보창 열 간격 | 53 | 60 |
| 정산 경험치 아이콘 | 48 | **46** (mark9는 24×23, 2배) |

### 색이 틀린 것

| 대상 | 포트 | 원본 | 근거 |
| --- | --- | --- | --- |
| 마법 카드(비활성) | (128,128,128) | `0x7f7f7f` | `MagickListLayer.js:66` |
| 마법 카드(MP 부족 비용) | 회색 | (139,33,33) | `MagickListLayer.js:67` |
| 지형 등급 색 네 단계 | 채널마다 ±1 | — | `TerrainLayer.js:110` |
| 지형 `--` 단계 | (199,199,199) | (32,32,32) | 같은 줄 |
| 지형 이름 | 크림/회색 | 검정 | `TerrainLayer.js:102` |
| 승리 조건 본문 | `#003fff` | `#0e01de` | 하네스 로그 |
| 「짐이 알겠다.」 | `#00d100` | `#025b00` | 하네스 로그 |

### 그리는 방식이 다른 것

- 지형 특기 칸: 포트는 `●`/`○` **글자**, 원본은 30×30 **아이콘** 4장 + 회색 material (`TerrainLayer.js:109`)
- 마법 피해 계수: 포트 `"1.0"`, 원본 `"1"` (JS 숫자 문자열이라 정수에 소수점이 없다) (`MagickListLayer.js:147-148`)

### 검증 장치가 거짓을 적던 것

- `auto-battle-active`: 확인창이 남은 화면을 위임 배너라고 기록
- 승리 조건 전체 창: 증거가 `listOf("승리 조건", "장보와 장량을", …)`로 **영천 문구를 하드코딩**

## 되풀이된 실패 형태 하나

수정한 결함 대부분이 **같은 이유로** 숨어 있었다.

> 같은 화면의 값이 **그리기와 증거 두세 곳에 따로** 적혀 있어, 각자 자기 값과 일관되면
> 게이트가 정직하게 돌면서도 아무것도 검증하지 못한다.

`auto-battle-active`는 더 나빴다. 2026-09-03의 초록은 화면이 맞아서가 아니라 **포트가
조건을 안 봐서** 났던 것이고, 포트가 원본에 맞게 조건을 보기 시작하자 빨개졌다.

**고친 방법**: 화면마다 계약 object 하나를 단일 출처로 두고 그리기와 증거가 둘 다 읽게
한다. 값은 언제나 **원본과 일치하는 쪽**을 정본으로 삼는다.

전투 렌더 기록기 16개 중 계약을 공유하는 것이 4개뿐이었고, 이번에 다음을 배선했다 —
`Mine/OtherUnitInfoRenderEvents`, `BattleCommandRenderEventRecorder`,
`BattleMagickRenderEventRecorder`, `TerrainLayerRenderEvents`, `BattleUsePropertyRenderEventRecorder`.

## 검증 규율 (다음 세션에서도 지킬 것)

1. **음성 대조 없이 초록을 믿지 않는다.** 계약 값을 하나 바꿔 경로가 **실제로 떨어지는지**
   확인하고 되돌린다. 떨어지지 않으면 배선이 겉도는 것이다. 이 절차로 여러 번 걸렀다.
2. **포트가 실제로 아는 색만 적는다.** 원본 하네스가 `#ffffff`를 낸다는 이유만으로 흰색을
   적으면 우연히 맞는 거짓말이다. 그리기 코드에서 `batch.color`/`font.color`가 어디서
   세워지고 어디서 바뀌는지 먼저 읽는다.
3. **타이밍 차이는 원본의 자체 편차부터 잰다.** 원본을 두 번 돌려 비교한다.
4. **편차가 좁아도 안심하지 않는다.** 캡처는 같은 의미 지점에서 찍히므로 오염이 결정적이다.
   클립 시계(trace 인덱스 15)와 창 안의 정체 프레임을 함께 본다.
5. **경계를 찾지 못하면 통과가 아니라 실패다.** `StopIteration`으로 멈추게 둔다.

## 구간 타이밍 판정 규칙을 고쳤다

첫 구간에서 두 창이 떨어졌는데 원인은 포트가 아니라 **원본 캡처의 정체**였다. 창 안에
0.34초·0.48초짜리 프레임(스크린샷 비용)이 있어 경계 프레임을 놓치고 창이 길게 측정됐다.

허용치를 **"고정 폭 + 양쪽 창의 정체 폭"**으로 바꿨다. 정체는 한 프레임이 0.05초를 넘는
경우만 센다 — 프레임마다의 작은 흔들림까지 더하면 체계적으로 느린 쪽을 흡수해 판정력이
사라진다. 정체가 없는 창은 허용치가 정확히 0.06으로 남는다.

이 규칙이 `477.py`·`first-round-end.py`가 손으로 들고 있던 `contaminated` 목록을 대신한다.
그 목록은 실행마다 손으로 고쳐야 했고, 정체가 없는 실행에서도 그 창을 영영 보지 않았다.

## 남은 일

| 항목 | 상태 |
| --- | --- |
| `postsCanEquip`의 WEAPONS·ARMOR·AUXILIARY 갈래 | posts 표의 EQUIP 비트와 아이템의 UPGRADE_ARM 목록이 포트 데이터 접근자에 없다. 값을 지어내지 않고 함수 주석에 원본 줄번호와 함께 적어 두었다 |
| 구간 타이밍 대조 나머지 8구간 | 스크립트는 모두 새 규칙으로 고쳤으나 실제 trace 대조는 5구간만 돌렸다 |
| `battle-view` 색 비교 0/16 | 전당 화면이라 영천 범위 밖 |
| 전투 기록기 7개의 손으로 적은 표 | 미니맵 `bg/box`, 편집 레이어 머리띠, 패배 프롬프트 `"비"` vs `"아니오"` 등 |
| 글꼴 치환 ~2px 오차 | 원본은 Chromium Arial, 포트는 Apple SD Gothic Neo. 우선순위 가장 낮음 |

## 작업 환경 주의

- 한 구간 trace가 **150~390MB**다. 디스크가 차 캡처가 실패한 적이 있다.
  사용자 승인을 받아 `verification/build/verification/`의 오래된 캡처 80개를 지워 9.9GB를
  확보했다(최신 6개 유지). 구간을 이어 돌리려면 공간을 계속 살펴야 한다.
- 원본과 포트를 **동시에 돌리지 않는다**. 포트가 도는 중에 gradle 빌드를 돌리면
  `core.jar`가 다시 쓰여 자산 읽기가 실패한다.
- `round3-210.py`는 `summarize`가 세 번 정의되지만 **마지막 판정부가 첫 블록의 `WALL_ONLY`를
  읽는다**. "중복 블록"으로 보고 지우면 `NameError`로 깨진다(실제로 깨뜨렸다가 되돌렸다).
