#!/usr/bin/env node
'use strict';

// Controlled source-engine fixture: call production StageLayer.delay and step
// an isolated cc.Scheduler with explicit deltas. This is not a natural run.
const {spawn}=require('node:child_process');
const crypto=require('node:crypto');
const fs=require('node:fs');
const http=require('node:http');
const path=require('node:path');

const sourceRoot=path.resolve(process.env.JOJO_SOURCE_ROOT||path.join(__dirname,'../../jojo_mobile/sgccz-desktop'));
const outputRoot=path.resolve(process.argv[2]||path.join(process.cwd(),'build/reports/opening-stage-delay-source'));
const port=Number(process.env.JOJO_CDP_PORT||9820+process.pid%70),deadlineMs=Number(process.env.JOJO_EXPORT_DEADLINE_MS||15000);
if(!Number.isFinite(port)||!Number.isFinite(deadlineMs)||deadlineMs<1000||deadlineMs>15000)throw Error('deadline must be 1000..15000ms and port numeric');
fs.mkdirSync(outputRoot,{recursive:true});
const delay=ms=>new Promise(r=>setTimeout(r,ms));
const shaFile=file=>crypto.createHash('sha256').update(fs.readFileSync(file)).digest('hex');
const getJson=url=>new Promise((resolve,reject)=>{const q=http.get(url,r=>{let b='';r.on('data',x=>b+=x);r.on('end',()=>{try{resolve(JSON.parse(b))}catch(e){reject(e)}})});q.setTimeout(1000,()=>q.destroy(Error('CDP HTTP timeout')));q.on('error',reject)});
async function poll(fn,label,end){let last;while(Date.now()<end){try{const x=await fn();if(x)return x}catch(e){if(e.fatal)throw e;last=e}await delay(20)}throw Error(`timeout waiting for ${label}: ${last||''}`)}
async function connect(url){const s=new WebSocket(url);await new Promise((r,j)=>{s.addEventListener('open',r,{once:true});s.addEventListener('error',j,{once:true})});let id=0;const waits=new Map();s.addEventListener('message',e=>{const m=JSON.parse(e.data),w=waits.get(m.id);if(w){waits.delete(m.id);m.error?w.reject(Error(JSON.stringify(m.error))):w.resolve(m.result)}});s.addEventListener('close',()=>{const e=Error('CDP closed before fixture completed');e.fatal=true;for(const w of waits.values())w.reject(e)});return{send(method,params={}){return new Promise((resolve,reject)=>{const n=++id;waits.set(n,{resolve,reject});s.send(JSON.stringify({id:n,method,params}))})},close(){s.close()}}}
const ready=`(()=>!!(globalThis.cc&&cc.director&&cc.Scheduler&&cc.js&&cc.js.getClassByName('StageLayer')))()`;
const probe=`(()=>{
 const StageLayer=cc.js.getClassByName('StageLayer');if(!StageLayer||typeof StageLayer.prototype.delay!=='function')throw Error('production StageLayer.delay unavailable');
 const f01=Math.fround(.1),snapshot=s=>s._arrayForTimers.flatMap(e=>(e.timers||[]).map(t=>({elapsed:t._elapsed,delay:t._delay,interval:t._interval,timesExecuted:t._timesExecuted,useDelay:t._useDelay,callbackActive:!!t._callback})));
 const run=(name,mode,deltas)=>{const scheduler=new cc.Scheduler(),originalScheduler=cc.director.getScheduler(),events=[],stage=Object.create(StageLayer.prototype);stage._skip=false;stage._script={pause(){events.push({kind:'pause',stepIndex})},resume(elapsed){events.push({kind:'resume',stepIndex,elapsed});if(mode==='chain'&&!chained){chained=true;StageLayer.prototype.delay.call(stage,3);events.push({kind:'chainDelayRegistered',stepIndex,timers:snapshot(scheduler)})}}};scheduler.enableForTarget(stage);let stepIndex=-1,chained=false,registered=false,registrar=null;try{cc.director.setScheduler(scheduler);if(mode==='outside'){StageLayer.prototype.delay.call(stage,3);registered=true}else if(mode==='chain'){StageLayer.prototype.delay.call(stage,3);registered=true}else{registrar={_id:'controlled-delay-registrar',update(){if(!registered){registered=true;StageLayer.prototype.delay.call(stage,3);events.push({kind:'registeredInsideUpdate',stepIndex})}}};scheduler.scheduleUpdate(registrar,0,false)}const steps=[];for(stepIndex=0;stepIndex<deltas.length;stepIndex++){const dt=deltas[stepIndex];scheduler.update(dt);steps.push({index:stepIndex,dt,timers:snapshot(scheduler),events:events.filter(x=>x.stepIndex===stepIndex)})}return{name,mode,deltas,steps,events,pauseCount:events.filter(x=>x.kind==='pause').length,resumeCount:events.filter(x=>x.kind==='resume').length}}finally{cc.director.setScheduler(originalScheduler)}};
 const cases=[run('outside-register-f32-tenths','outside',[10,f01,f01,f01]),run('register-during-update-f32-tenths','inside-update',[10,f01,f01,f01]),run('chain-register-no-remainder','chain',[10,f01,f01,f01,f01,f01,f01])];
 return {engineVersion:cc.ENGINE_VERSION||null,floatPointOne:f01,cases};
})()`;
(async()=>{const expires=Date.now()+deadlineMs,startedAt=new Date();try{await getJson(`http://127.0.0.1:${port}/json/list`);throw Error(`CDP port ${port} busy`)}catch(e){if(e.code!=='ECONNREFUSED')throw e}const log=fs.openSync(path.join(outputRoot,'source-process.log'),'w'),child=spawn('npm',['exec','--','electron',path.join(__dirname,'hold_source_verification_exit.cjs'),'--verify-desktop',`--remote-debugging-port=${port}`],{cwd:sourceRoot,env:{...process.env,JOJO_SOURCE_MAIN:path.join(sourceRoot,'electron/main.cjs')},stdio:['ignore',log,log],detached:true});let client,childExit=null;child.once('exit',(code,signal)=>childExit={code,signal});const timer=setTimeout(()=>{try{process.kill(-child.pid,'SIGTERM')}catch{}setTimeout(()=>{try{process.kill(-child.pid,'SIGKILL')}catch{}process.exit(124)},250)},deadlineMs);try{const page=await poll(async()=>{if(childExit){const e=Error(`source exited: ${JSON.stringify(childExit)}`);e.fatal=true;throw e}const pages=await getJson(`http://127.0.0.1:${port}/json/list`);return pages.find(x=>x.type==='page'&&x.webSocketDebuggerUrl)},'source page',Math.min(expires,Date.now()+7000));if(page.url!=='sgccz://game/electron/index.html')throw Error(`unexpected source page ${page.url}`);client=await connect(page.webSocketDebuggerUrl);const evaluate=async expression=>{const r=await client.send('Runtime.evaluate',{expression,returnByValue:true,awaitPromise:true});if(r.exceptionDetails)throw Error(JSON.stringify(r.exceptionDetails));return r.result.value};await poll(()=>evaluate(ready),'StageLayer class',Math.min(expires,Date.now()+7000));const result=await evaluate(probe);const stageLayer=path.join(sourceRoot,'recovered-js/modules/ui/StageLayer.js'),engine=path.join(sourceRoot,'web/cocos2d-js.js');if(result.cases.length!==3)throw Error('incomplete controlled delay fixture');const [outside,inside,chain]=result.cases;if(outside.steps[0].timers[0]?.elapsed!==0||outside.resumeCount!==1||outside.events.find(x=>x.kind==='resume')?.stepIndex!==3||inside.steps[0].timers[0]?.elapsed!==0||inside.events.find(x=>x.kind==='registeredInsideUpdate')?.stepIndex!==0||inside.events.find(x=>x.kind==='resume')?.stepIndex!==3||chain.steps[3].timers[0]?.elapsed!==0||chain.events.filter(x=>x.kind==='resume').map(x=>x.stepIndex).join(',')!=='3,6')throw Error(`controlled delay behavior changed: ${JSON.stringify(result.cases)}`);const report={contract:'controlled-source-stage-delay-steps-v1',evidenceKind:'actual-source-engine-production-stage-delay-controlled-delta',controlledEvidence:true,naturalRun:false,sourceRoot,startedAt:startedAt.toISOString(),completedAt:new Date().toISOString(),source:{stageLayer:{path:stageLayer,sha256:shaFile(stageLayer)},engine:{path:engine,sha256:shaFile(engine)}},...result};fs.writeFileSync(path.join(outputRoot,'source-stage-delay-steps.json'),JSON.stringify(report,null,2)+'\n');console.log(`SOURCE_STAGE_DELAY_STEPS_OK cases=${result.cases.length}`)}finally{clearTimeout(timer);if(client)client.close();try{process.kill(-child.pid,'SIGTERM')}catch{}await delay(300);try{process.kill(-child.pid,'SIGKILL')}catch{}fs.closeSync(log)}})().catch(e=>{console.error(e.stack||e);process.exitCode=1});
